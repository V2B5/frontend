import React from 'react';
import EventoList from '../../views/eventos/EventList';

const Eventos = () => {
    return (
        <div>
            <EventoList />
        </div>
    );
};

export default Eventos;